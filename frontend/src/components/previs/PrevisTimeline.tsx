"use client";

// Re-exported for backwards compatibility — the transport bar was extracted
// into PrevisTransport.tsx as part of the UI redesign.
export { PrevisTransport as PrevisTimeline } from "./PrevisTransport";
